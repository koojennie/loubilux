// const express = require('express');
// const router = express.Router();

// const {createReview, getProductReviews, getUserReviews, getAllReviews , updateReviewProduct, deleteReviewProduct} = require('../controllers/review.controller');
// const {authenticateUser, authorizeRoles} = require('../middleware/auth.middleware');

// router.post('/post', authenticateUser, createReview);
// router.get('/product/:productId', authenticateUser, getProductReviews);
// router.get('/user',authenticateUser, getUserReviews);
// router.get('/all', getAllReviews);
// router.put('/:reviewId', authenticateUser, updateReviewProduct);
// router.delete('/:reviewId', authenticateUser, deleteReviewProduct);

// module.exports = router;